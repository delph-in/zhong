;;; -*- Mode: TDL; -*-
;;; files to load

grammar-top               := "../zhs-pet-strict.tdl".
generation-ignore-signs   := "../generation.ignore".
parsing-roots             := root.

:begin :config.
 :include "config.common".
:end :config.

